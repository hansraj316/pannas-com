import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pannas.com | Premium Global Pan Delivery",
  description: "Experience the authentic taste of premium betel leaf preparations, hand-crafted and delivered to over 128 countries within 48 hours.",
  keywords: "pan, meeta pan, sada pan, kolkata meeta, banaras meeta, daisy mehta, global pan delivery",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
